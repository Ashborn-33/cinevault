# Auth Feature Directory

This folder is reserved for CineVault's authentication elements.

## Future Layout
- `components/`: UI components (LoginCard, RegisterCard, OAuthButtons)
- `hooks/`: Auth state hooks (useAuth, useSession)
- `services/`: API connectors to Supabase Auth actions
